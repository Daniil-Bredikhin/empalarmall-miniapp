import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => <h1>EMPALAR MALL Mini App</h1>;

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(<App />);
